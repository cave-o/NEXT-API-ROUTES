export default interface IComments {
    text: string;
    id:number 
}
export default interface ITours {
    text: string;
    id:number 
}

export default interface IReviews {
    text: string;
    id:number 
}